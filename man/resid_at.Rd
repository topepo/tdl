% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_resid_at.R
\name{resid_at}
\alias{resid_at}
\title{Residual connection locations}
\usage{
resid_at(range = c(2L, dials::unknown()), trans = NULL)
}
\arguments{
\item{range}{A two-element vector with the lower and upper bounds. The upper
bound defaults to \code{\link[dials:unknown]{dials::unknown()}} and must be finalized before grid
creation, e.g. with \code{\link[=update]{update()}}.}

\item{trans}{A transformation object (default \code{NULL} for no transformation).}
}
\value{
A \code{param} object.
}
\description{
The layer indices at which residual connections are added in a tabular
residual network.
}
\details{
Used as a tuning parameter for \code{\link[=tab_resnet]{tab_resnet()}} when fit with the \code{"brulee"}
engine. The upper bound depends on the number of hidden layers in the
network and so is left as \code{unknown()} by default.
}
\examples{
resid_at()
resid_at(range = c(2L, 6L))

}
