% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_num_attn_blocks.R
\name{num_attn_blocks}
\alias{num_attn_blocks}
\title{Number of attention blocks}
\usage{
num_attn_blocks(range = c(1L, 6L), trans = NULL)
}
\arguments{
\item{range}{A two-element vector with the lower and upper bounds.}

\item{trans}{A transformation object (default \code{NULL} for no transformation).}
}
\value{
A \code{param} object.
}
\description{
The number of sequential attention blocks in the model architecture.
}
\details{
Used as a tuning parameter for \code{\link[=tabular_auto_int]{tabular_auto_int()}} when fit with the
\code{"brulee"} engine. Each attention block consists of a multi-head attention
layer followed by feed-forward layers.
}
\examples{
num_attn_blocks()
num_attn_blocks(range = c(1L, 4L))

}
