% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_int.R, R/tabular_pfn.R,
%   R/tabular_resnet.R, R/tabular_rln.R
\name{update.tabular_auto_int}
\alias{update.tabular_auto_int}
\alias{update.tabular_pfn}
\alias{update.tabular_resnet}
\alias{update.tabular_rln}
\title{Updating a model specification}
\usage{
\method{update}{tabular_auto_int}(
  object,
  parameters = NULL,
  epochs = NULL,
  num_embedding = NULL,
  hidden_units = NULL,
  hidden_activations = NULL,
  num_attn_feat = NULL,
  num_attn_heads = NULL,
  num_attn_blocks = NULL,
  activation = NULL,
  dropout = NULL,
  dropout_attn = NULL,
  dropout_embedding = NULL,
  penalty = NULL,
  mixture = NULL,
  learn_rate = NULL,
  rate_schedule = NULL,
  momentum = NULL,
  batch_size = NULL,
  class_weights = NULL,
  stop_iter = NULL,
  fresh = FALSE,
  ...
)

\method{update}{tabular_pfn}(
  object,
  parameters = NULL,
  num_estimators = NULL,
  softmax_temperature = NULL,
  balance_probabilities = NULL,
  average_before_softmax = NULL,
  fresh = FALSE,
  ...
)

\method{update}{tabular_resnet}(
  object,
  parameters = NULL,
  hidden_units = NULL,
  bottleneck_units = NULL,
  resid_at = NULL,
  penalty = NULL,
  dropout = NULL,
  epochs = NULL,
  activation = NULL,
  learn_rate = NULL,
  fresh = FALSE,
  ...
)

\method{update}{tabular_rln}(
  object,
  parameters = NULL,
  hidden_units = NULL,
  penalty_type = NULL,
  penalty_average = NULL,
  step_rate = NULL,
  activation = NULL,
  epochs = NULL,
  learn_rate = NULL,
  rate_schedule = NULL,
  momentum = NULL,
  batch_size = NULL,
  fresh = FALSE,
  ...
)
}
\arguments{
\item{object}{A \link[parsnip:model_spec]{model specification}.}

\item{parameters}{A 1-row tibble or named list with \emph{main}
parameters to update. Use \strong{either} \code{parameters} \strong{or} the main arguments
directly when updating. If the main arguments are used,
these will supersede the values in \code{parameters}. Also, using
engine arguments in this object will result in an error.}

\item{epochs}{An integer for the number of training iterations.}

\item{num_embedding}{An integer for the dimensionality of the embedding space
for features.}

\item{hidden_units}{An integer vector for the number of units in the hidden
layers after the attention mechanism.}

\item{hidden_activations}{A character vector denoting the activation functions
for the hidden layers.}

\item{num_attn_feat}{An integer for the number of attention features.}

\item{num_attn_heads}{An integer for the number of attention heads in the
multi-head attention mechanism.}

\item{num_attn_blocks}{An integer for the number of sequential attention
blocks.}

\item{activation}{A single character string denoting the type of relationship
between the original predictors and the hidden unit layer. The activation
function between the hidden and output layers is automatically set to either
"linear" or "softmax" depending on the type of outcome. Possible values
depend on the engine being used.}

\item{dropout}{A number between 0 (inclusive) and 1 denoting the proportion
of model parameters randomly set to zero during model training.}

\item{dropout_attn}{A number between 0 (inclusive) and 1 denoting the
proportion of attention weights set to zero during model training.}

\item{dropout_embedding}{A number between 0 (inclusive) and 1 denoting the
proportion of embedding values set to zero during model training.}

\item{penalty}{A non-negative numeric value for the amount of weight
decay.}

\item{mixture}{A number between zero and one (inclusive) denoting the
proportion of L1 regularization (i.e. lasso) in the model.
\itemize{
\item \code{mixture = 1} specifies a pure lasso model,
\item \code{mixture = 0}  specifies a ridge regression model, and
\item \verb{0 < mixture < 1} specifies an elastic net model, interpolating lasso and ridge.
}

Available for specific engines only.}

\item{learn_rate}{A number for the rate at which the boosting algorithm adapts
from iteration-to-iteration (specific engines only). This is sometimes referred to
as the shrinkage parameter.}

\item{rate_schedule}{A character string for the learning rate schedule.}

\item{momentum}{A number for the momentum parameter in optimizers that use it.}

\item{batch_size}{An integer for the number of training instances in each
batch.}

\item{class_weights}{Numeric class weights for imbalanced data
(classification only).}

\item{stop_iter}{The number of iterations without improvement before
stopping (specific engines only).}

\item{fresh}{A logical for whether the arguments should be
modified in-place or replaced wholesale.}

\item{...}{Not used for \code{update()}.}

\item{num_estimators}{An integer for the ensemble size. Default is \code{8L}.}

\item{softmax_temperature}{An adjustment factor that is a divisor in the
exponents of the softmax function (see \code{\link[tabpfn:tab_pfn]{tabpfn::tab_pfn()}}). Defaults to 0.9.}

\item{balance_probabilities}{A logical to adjust the prior probabilities in
cases where there is a class imbalance. Default is \code{FALSE}. Classification
only.}

\item{average_before_softmax}{A logical. For cases where
\code{num_estimators > 1}, should the average be done before using the softmax
function or after? Default is \code{FALSE}.}

\item{bottleneck_units}{The number of embeddings that are produced by batch
normalization.}

\item{resid_at}{An integer vector with the layer number should use a
residual connection (i.e., skip layer).}

\item{penalty_type}{A string for the regularization norm: \code{"L1"} (default
in \code{brulee}) or \code{"L2"}. L1 is recommended by the original paper.}

\item{penalty_average}{A positive numeric value for the target geometric mean
of the per-weight regularization coefficients, on the natural scale.
Best tuned on the log10 scale via \code{\link[=penalty_average]{penalty_average()}}.}

\item{step_rate}{A positive numeric value for the step size used to update
the per-weight regularization coefficients, on the natural scale.
Best tuned on the log10 scale via \code{\link[=step_rate]{step_rate()}}.}
}
\description{
Updating a model specification
}
